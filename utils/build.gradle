apply plugin: 'anx-kotlin'
apply plugin: 'anx-jar-publish'

dependencies {
   compile Deps.ktStdlib
   compile Deps.ktJava8
   compile Deps.slf4j

   testCompile Deps.standardTest
   testCompile Deps.logback
}
